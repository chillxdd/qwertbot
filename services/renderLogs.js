'use strict';

// The Render log API integration was intentionally removed. Runtime health is
// collected locally through /runtime-diagnostics, so QwertBot no longer needs
// a Render API key or makes outbound requests to api.render.com.
module.exports = {};
